# Fourier Series of f(x) = x + π

## Problem Statement

Find the Fourier series of

```math
f(x)=x+\pi,\qquad -\pi \le x \le \pi
```

---

## Step 1: Fourier Series Formula

For a function defined on the interval ([-\pi,\pi]), the Fourier series is

```math
f(x)=\frac{a_0}{2}
+\sum_{n=1}^{\infty}
\left(
a_n\cos(nx)+b_n\sin(nx)
\right)
```

where

```math
a_0=\frac{1}{\pi}
\int_{-\pi}^{\pi}
f(x)\,dx
```

```math
a_n=\frac{1}{\pi}
\int_{-\pi}^{\pi}
f(x)\cos(nx)\,dx
```

```math
b_n=\frac{1}{\pi}
\int_{-\pi}^{\pi}
f(x)\sin(nx)\,dx
```

---

## Step 2: Determine the Constant Coefficient

Substitute (f(x)=x+\pi):

```math
a_0
=
\frac{1}{\pi}
\int_{-\pi}^{\pi}
(x+\pi)\,dx
```

Separate the integral:

```math
a_0
=
\frac{1}{\pi}
\left(
\int_{-\pi}^{\pi}x\,dx
+
\int_{-\pi}^{\pi}\pi\,dx
\right)
```

Since (x) is an odd function,

```math
\int_{-\pi}^{\pi}x\,dx=0
```

and

```math
\int_{-\pi}^{\pi}\pi\,dx
=
2\pi^2
```

Therefore,

```math
a_0
=
\frac{2\pi^2}{\pi}
=
2\pi
```

Hence,

```math
\frac{a_0}{2}
=
\pi
```

---

## Step 3: Determine the Cosine Coefficients

Using the definition,

```math
a_n
=
\frac{1}{\pi}
\int_{-\pi}^{\pi}
(x+\pi)\cos(nx)\,dx
```

Split the integral:

```math
a_n
=
\frac{1}{\pi}
\left[
\int_{-\pi}^{\pi}
x\cos(nx)\,dx
+
\pi
\int_{-\pi}^{\pi}
\cos(nx)\,dx
\right]
```

Because (x) is odd and (\cos(nx)) is even,

```math
x\cos(nx)
```

is an odd function, so

```math
\int_{-\pi}^{\pi}
x\cos(nx)\,dx
=
0
```

Also,

```math
\int_{-\pi}^{\pi}
\cos(nx)\,dx
=
\left[
\frac{\sin(nx)}{n}
\right]_{-\pi}^{\pi}
=
0
```

Thus,

```math
a_n=0
```

---

## Step 4: Determine the Sine Coefficients

Using the definition,

```math
b_n
=
\frac{1}{\pi}
\int_{-\pi}^{\pi}
(x+\pi)\sin(nx)\,dx
```

The constant term contributes nothing because

```math
\int_{-\pi}^{\pi}
\sin(nx)\,dx
=
0
```

Therefore,

```math
b_n
=
\frac{1}{\pi}
\int_{-\pi}^{\pi}
x\sin(nx)\,dx
```

Since both (x) and (\sin(nx)) are odd functions, their product is even:

```math
b_n
=
\frac{2}{\pi}
\int_{0}^{\pi}
x\sin(nx)\,dx
```

### Integration by Parts

Let

```math
u=x,
\qquad
dv=\sin(nx)\,dx
```

Then

```math
du=dx,
\qquad
v=-\frac{\cos(nx)}{n}
```

Apply integration by parts:

```math
\int_{0}^{\pi}
x\sin(nx)\,dx
=
-\frac{x\cos(nx)}{n}
\Bigg|_{0}^{\pi}
+
\frac{1}{n}
\int_{0}^{\pi}
\cos(nx)\,dx
```

The remaining integral equals

```math
\int_{0}^{\pi}
\cos(nx)\,dx
=
\frac{\sin(n\pi)}{n}
=
0
```

Therefore,

```math
\int_{0}^{\pi}
x\sin(nx)\,dx
=
-\frac{\pi\cos(n\pi)}{n}
```

Since

```math
\cos(n\pi)=(-1)^n
```

we obtain

```math
\int_{0}^{\pi}
x\sin(nx)\,dx
=
-\frac{\pi(-1)^n}{n}
```

Substituting into the formula for (b_n):

```math
b_n
=
\frac{2}{\pi}
\left(
-\frac{\pi(-1)^n}{n}
\right)
```

which simplifies to

```math
b_n
=
\frac{2(-1)^{n+1}}{n}
```

---

## Step 5: Construct the Fourier Series

The Fourier coefficients are

```math
a_0=2\pi
```

```math
a_n=0
```

```math
b_n=
\frac{2(-1)^{n+1}}{n}
```

Substituting them into the Fourier series formula:

```math
f(x)
=
\pi
+
\sum_{n=1}^{\infty}
\frac{2(-1)^{n+1}}{n}
\sin(nx)
```

---

## Final Answer

```math
f(x)
=
x+\pi
=
\pi
+
2
\sum_{n=1}^{\infty}
\frac{(-1)^{n+1}}{n}
\sin(nx)
```
