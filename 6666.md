# Fourier Series of f(x) = x + π

## Problem Statement

Find the Fourier series of

```math
f(x)=x+\pi,
\qquad
-\pi\le x\le\pi
```

---

## Step 1: Define the Function

The given function is

```math
f(x)=x+\pi
```

defined on the interval

```math
[-\pi,\pi]
```

Our goal is to represent this function as a Fourier series.

---

## Step 2: Write the General Fourier Series Formula

For a function defined on

```math
[-\pi,\pi]
```

the Fourier series is

```math
f(x)
=
\frac{a_0}{2}
+
\sum_{n=1}^{\infty}
\left(
a_n\cos(nx)
+
b_n\sin(nx)
\right)
```

---

## Step 3: Write the Formula for the Constant Coefficient

The constant coefficient is

```math
a_0
=
\frac{1}{\pi}
\int_{-\pi}^{\pi}
f(x)\,dx
```

Substituting the given function:

```math
a_0
=
\frac{1}{\pi}
\int_{-\pi}^{\pi}
(x+\pi)\,dx
```

---

## Step 4: Separate the Integral

Split the integral into two parts:

```math
a_0
=
\frac{1}{\pi}
\left(
\int_{-\pi}^{\pi}
x\,dx
+
\int_{-\pi}^{\pi}
\pi\,dx
\right)
```

---

## Step 5: Evaluate the First Integral

Since

```math
x
```

is an odd function,

```math
\int_{-\pi}^{\pi}
x\,dx
=
0
```

---

## Step 6: Evaluate the Second Integral

Since

```math
\pi
```

is a constant,

```math
\int_{-\pi}^{\pi}
\pi\,dx
=
\pi(2\pi)
=
2\pi^2
```

Therefore,

```math
a_0
=
\frac{2\pi^2}{\pi}
=
2\pi
```

---

## Step 7: Obtain the Constant Term

The constant term in the Fourier series is

```math
\frac{a_0}{2}
=
\pi
```

---

## Step 8: Write the Formula for the Cosine Coefficients

The cosine coefficients are

```math
a_n
=
\frac{1}{\pi}
\int_{-\pi}^{\pi}
(x+\pi)\cos(nx)\,dx
```

---

## Step 9: Split the Expression for aₙ

```math
a_n
=
\frac{1}{\pi}
\left[
\int_{-\pi}^{\pi}
x\cos(nx)\,dx
+
\pi
\int_{-\pi}^{\pi}
\cos(nx)\,dx
\right]
```

---

## Step 10: Evaluate the First Integral in aₙ

Since

```math
x
```

is odd and

```math
\cos(nx)
```

is even,

```math
x\cos(nx)
```

is odd.

Therefore,

```math
\int_{-\pi}^{\pi}
x\cos(nx)\,dx
=
0
```

---

## Step 11: Evaluate the Second Integral in aₙ

```math
\int_{-\pi}^{\pi}
\cos(nx)\,dx
=
\left[
\frac{\sin(nx)}{n}
\right]_{-\pi}^{\pi}
=
0
```

Therefore,

```math
a_n=0
```

---

## Step 12: Write the Formula for the Sine Coefficients

The sine coefficients are

```math
b_n
=
\frac{1}{\pi}
\int_{-\pi}^{\pi}
(x+\pi)\sin(nx)\,dx
```

---

## Step 13: Eliminate the Constant Contribution

Because

```math
\sin(nx)
```

is odd,

```math
\int_{-\pi}^{\pi}
\sin(nx)\,dx
=
0
```

Therefore,

```math
b_n
=
\frac{1}{\pi}
\int_{-\pi}^{\pi}
x\sin(nx)\,dx
```

---

## Step 14: Use Symmetry

Since both

```math
x
```

and

```math
\sin(nx)
```

are odd functions,

```math
x\sin(nx)
```

is even.

Hence,

```math
b_n
=
\frac{2}{\pi}
\int_{0}^{\pi}
x\sin(nx)\,dx
```

---

## Step 15: Apply Integration by Parts

Let

```math
u=x
```

and

```math
dv=\sin(nx)\,dx
```

Then

```math
du=dx
```

and

```math
v=-\frac{\cos(nx)}{n}
```

Applying integration by parts,

```math
\int_{0}^{\pi}
x\sin(nx)\,dx
=
-\frac{x\cos(nx)}{n}
\Bigg|_{0}^{\pi}
+
\frac{1}{n}
\int_{0}^{\pi}
\cos(nx)\,dx
```

---

## Step 16: Evaluate the Remaining Integral

```math
\int_{0}^{\pi}
\cos(nx)\,dx
=
\frac{\sin(n\pi)}{n}
=
0
```

Therefore,

```math
\int_{0}^{\pi}
x\sin(nx)\,dx
=
-\frac{\pi\cos(n\pi)}{n}
```

---

## Step 17: Simplify the Result

Using

```math
\cos(n\pi)
=
(-1)^n
```

we obtain

```math
\int_{0}^{\pi}
x\sin(nx)\,dx
=
-\frac{\pi(-1)^n}{n}
```

---

## Step 18: Compute bₙ

Substituting into the formula for

```math
b_n
```

gives

```math
b_n
=
\frac{2}{\pi}
\left(
-\frac{\pi(-1)^n}{n}
\right)
```

Thus,

```math
b_n
=
\frac{2(-1)^{n+1}}{n}
```

---

## Step 19: Construct the Fourier Series

The Fourier coefficients are

```math
a_0=2\pi
```

```math
a_n=0
```

```math
b_n
=
\frac{2(-1)^{n+1}}{n}
```

Substituting them into the Fourier series formula:

```math
f(x)
=
\pi
+
\sum_{n=1}^{\infty}
\frac{2(-1)^{n+1}}{n}
\sin(nx)
```

---

## Step 20: Final Answer

```math
f(x)
=
x+\pi
=
\pi
+
2
\sum_{n=1}^{\infty}
\frac{(-1)^{n+1}}{n}
\sin(nx)
```
